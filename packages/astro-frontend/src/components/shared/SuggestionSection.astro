---
import { BootstrapItaliaIcon } from "./BootstrapItaliaIcon";
// import { useUiTranslations } from "../../i18n/ui.i18n";
import { Section } from "./Section";
import {
  getLangFromUrl,
  // getRouteKeyFromCurrentRoutePattern,
} from "../../i18n/utils.i18n";
import { strapiService } from "../../server/services";

// type SuggestionCard = {
//   title: string;
//   description: string;
//   buttonLink: {
//     href: string;
//     label: string;
// mixpanelAttributes?: {
//   "data-mp-external-link-id": string;
//   "data-mp-external-link-description": string;
// };
//   };
//   externalLink: boolean;
//   imageSrc: string;
// };

// type Props = {
//   leftCard: SuggestionCard;
//   rightCard: SuggestionCard;
// };

const currentLocale = getLangFromUrl(Astro.url.pathname);
// const t = useUiTranslations(currentLocale);

// const routeKey = getRouteKeyFromCurrentRoutePattern(Astro.routePattern);

const strapiResponse = await strapiService.getGeneralContent(currentLocale);

if (!strapiResponse) {
  throw new Error("Could not fetch content from strapi");
}

const contents = strapiResponse.data.HowToSection;

// const suggestions = contents.HowTo.map((suggestion, index) => {
//   const mixpanelAttributes =
//     index === 0
//       ? {
//           "data-mp-external-link-id": `${routeKey}_suggestionSection_leftCard_interopDevelopersLink`,
//           "data-mp-external-link-description": "TODO",
//         }
//       : undefined;
//   return {
//     ...suggestion,
//     mixpanelAttributes: mixpanelAttributes,
//   };
// });

// const { leftCard: providedLeftCard, rightCard: providedRightCard } =
//   Astro.props as Props;

// const suggestions = [providedLeftCard, providedRightCard] as const;

// TODO remove strings and comments
// t("suggestion_section.title")
---

<div class="pb-4" id="suggestion-section">
  <Section
    style={{ maxWidth: "1076px" }}
    className="mx-auto"
    title={contents.Title}
    titleClass="text-white"
  >
    <div class="d-flex flex-column flex-lg-row gap-4">
      {
        contents.HowTo.map((suggestion) => (
          <article class="bg-white rounded shadow-sm p-5" style="flex: 1;">
            <img
              style="fill: var(--bs-primary);"
              src={suggestion.Illustration.url}
              class="mb-4 color-primary"
              alt={suggestion.Illustration.alternativeText}
              loading="lazy"
              role="presentation"
              width="96"
              height="96"
            />
            <h3 class="h4 mb-3">{suggestion.Title}</h3>
            <p class="it-card-text text-secondary mb-3">
              {suggestion.Description}
            </p>
            <footer>
              <a
                href={suggestion.LinkURL}
                class="btn btn-primary btn-icon"
                data-mp-external-link-id={suggestion.MixpanelExternalLinkId}
                data-mp-external-link-description={
                  suggestion.MixpanelExternalLinkDescription
                }
                {...(!suggestion.IsLinkInternal
                  ? { target: "_blank", rel: "noopener noreferrer" }
                  : {})}
              >
                {suggestion.LinkLabel}
                {!suggestion.IsLinkInternal && (
                  <BootstrapItaliaIcon
                    className="ms-2"
                    name="it-external-link"
                    color="white"
                  />
                )}
              </a>
            </footer>
          </article>
        ))
      }
    </div>
  </Section>
</div>

<style>
  #suggestion-section {
    background: linear-gradient(to bottom, var(--bs-primary), #004d99 100%);
  }
</style>
