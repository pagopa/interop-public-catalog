---
export const prerender = false

import Layout from '../../../layouts/Layout.astro'
import Header from '../../../components/Header'
import Footer from '../../../components/Footer'
import Pagination from '../../../components/Pagination/Pagination'
import EServiceCatalog from '../../../components/EServiceCatalog/EServiceCatalog'
import { Breadcrumb, BreadcrumbItem, Button, Container, Icon, Section } from 'design-react-kit'
import SuggestionCard from '../../../components/SuggestionsSection/SuggestionCard'
---

<Layout>
  <Header client:load />

  <Section>
    <Breadcrumb>
      <BreadcrumbItem>
        <a href="#"> Home </a>
        <span class="separator"> / </span>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <a href="#"> Subsection </a>
        <span class="separator"> / </span>
      </BreadcrumbItem>
      <BreadcrumbItem active> Current section </BreadcrumbItem>
    </Breadcrumb>

    <h1>Esplora il catalogo</h1>
    <span
      >Cerca tra tutte le API rese disponibili attraverso la Piattaforma Digitale Nazionale Dati
      (PDND). Verifica i requisiti per accedere alle API di tuo interesse e costruisci la tua
      integrazione per l’accesso ai dati.</span
    >

    <Container className="primary-bg-c1 p-3">
      <div>Qua ci staranno i filtri</div>
    </Container>

    <EServiceCatalog />
    <Pagination client:load currentPage={3} totalPages={7} />
  </Section>

  <Section color="muted">
    <h3>Cerchi l’elenco completo degli aderenti e delle API disponibili su PDND?</h3>
    <div class="d-flex flex-row">
      <div class="me-2">
        <Button color="primary" outline size="sm" onClick={(): void => {}} icon
          >Ottieni l’elenco delle API <Icon color="white" icon="it-arrow-right" /></Button
        >
      </div>
      <Button color="primary" outline size="sm" onClick={(): void => {}} icon
        >Ottieni l’elenco degli aderenti <Icon color="white" icon="it-arrow-right" /></Button
      >
    </div>
  </Section>

  <Section color="primary">
    <h3 class="text-white">Cosa fare per...</h3>
    <ul class="list-group list-group-horizontal styled-none">
      <li class="flex-fill list-group-item bg-transparent border-0">
        <SuggestionCard
          client:load
          icon="/Group.svg"
          title="Suggestion 1"
          description="Suggestion 1 description"
          actionButton={{ label: 'Action', onClick: (): void => {} }}
        />
      </li>
      <li class="flex-fill list-group-item bg-transparent border-0">
        <SuggestionCard
          icon="/Group.svg"
          title="Suggestion 1"
          description="Suggestion 1 description"
          actionButton={{ label: 'Action', onClick: (): void => {} }}
        />
      </li>
    </ul>
  </Section>

  <Footer client:load />
</Layout>
